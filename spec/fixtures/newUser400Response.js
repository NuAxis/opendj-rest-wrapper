'use strict';

var json = {
  'code': 400,
  'reason': 'Bad Request',
  'message': 'The request cannot be processed because it attempts to remove the required field \' / _id \'',
};

module.exports = json;
